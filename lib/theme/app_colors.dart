import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF176B73); // Deep Teal / Forest
  static const Color background = Color(0xFFF8F7F2); // Warm Off-White / Cream
  static const Color sage = Color(0xFF7FA58A); // Sage Green
  static const Color sand = Color(0xFFDCC7A1); // Warm Sand
  
  // Text Colors
  static const Color textPrimary = Color(0xFF263238); // Deep Charcoal
  static const Color textSecondary = Color(0xFF546E7A); // Lighter Charcoal
  static const Color textLight = Color(0xFFFFFFFF);
  
  // Status Colors
  static const Color warning = Color(0xFFE6A23C); // Soft Amber
  static const Color emergency = Color(0xFFD95C5C); // Muted Coral
  static const Color safe = Color(0xFF7FA58A); // Reusing Sage for safe
  
  // Neutral/UI Colors
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color cardSageBackground = Color(0xFFE6EBE4); // Very light sage for cards seen in images
  static const Color divider = Color(0xFFE0E0E0);
  
  // Image overlays
  static const Color imageOverlay = Colors.black26;
}
